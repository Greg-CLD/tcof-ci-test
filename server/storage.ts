/**
 * Server-side storage utilities
 * 
 * This file isn't actually used since all storage happens client-side via localStorage,
 * but it's created to satisfy the expected import in routes.ts
 */

// Empty interface to be expanded if needed
export const storage = {
  // Placeholder methods
};
